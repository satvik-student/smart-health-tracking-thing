# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/healthtracker

# For MongoDB Atlas (cloud), use this format instead:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/healthtracker

# Server Configuration
PORT=4000

# Firebase Configuration (for later use)
# FIREBASE_API_KEY=your_api_key
# FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
# FIREBASE_PROJECT_ID=your_project_id

# SMS Configuration (for later use)
# FAST2SMS_API_KEY=your_fast2sms_key